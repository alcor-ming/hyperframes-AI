# Baseline evidence

The Open Design revision and its SHA-256 remain the visual evidence for this
release. No private Work frames are copied into the public package.

The release asserts contract and byte-level integrity here. Add sanitized
1440x1080 keyframes only when a later QA task supplies them; a frozen visual
change requires a new component version.
