---
{"status":"draft","revision":1,"template":"__TEMPLATE__","profile":"__PROFILE__","ratio":"__RATIO__","target_platform":null,"script_revision":__SCRIPT_REVISION__,"research_revision":__RESEARCH_REVISION__,"timing_source":"estimated","subject_position":__SUBJECT_POSITION__}
---

# Animation Plan

> 读取实际 Script、Research、音频与已有成果后填写；保留原 Scene/Anchor ID，不批量重编号或改写 Accepted Snapshot。
> Script 决定讲什么；Research 提供有依据的候选内容与素材；本 Plan 自主取舍并决定如何呈现，不把研究文字当作必上屏稿件。发现具体材料需求时定点补 Research，不重开全轮；取舍改变已确认观点或叙事时才提交局部确认，已批准视觉方向的变更沿用既有规则。
>
> 阶段与接受只见 `.studio/workflow.md`，表达检查见 `.studio/spec/visual-design.md`。本模板记录实际设计，不另建 Shotbook、第二份时间真源或逐 Scene 审批表。

## 1. 作品与版本

| 项目 | 当前值 |
|---|---|
| Work / Variant | `<实际 ID>` |
| Plan revision / 基准版本 | `<新修订及所基于的当前可编辑版本>` |
| Script / Research 引用 | `<实际文件与 revision>` |
| 画幅 / 输出 fps | `<沿用当前设置；未确定则明确写预算>` |
| 视觉主题 | `<选定预制主题及工程参数引用；保留已指定品牌，不强制 Profile>` |
| 背景 / 运动 | `<独立 Background 精确引用；Motion 槽位引用或显式禁用；不由 Theme 隐式决定>` |
| 叙事模式 | `<文字主导 / 动效主导；IP Beta 按实际目标，不机械套标准模式>` |
| 主音频 / 对齐来源 | `<文件与对齐引用；无最终音频标 provisional>` |
| 现有成果迁移记录 | `<已有任务记录位置；不在这里重复维护全资产清单>` |
| 当前工具根 / 版本记录 | `<实际 WIN_ROOT 与部署身份的引用；无 Harness session>` |

**作品目标：** `<观众看完应理解什么。只写本片真实目标。>`

**本次必须保留：** `<已确认观点、画面、对象、录音和效果。没有就省略。>`

**时间现状：** `<已对齐最终音频 / 已有音频但关键 cue 未验证 / 临时音轨 / 仅预算。>`

## 2. 整片概览

按 `.studio/spec/visual-design.md` 确定本片 A/B 职责；仅记录实际安排，不复制默认区间。主声源贯穿切换且只播放一次。

**本片的主要叙述方式：** `<实际方式>`

**本片主要补充机制：** `<需要补充什么，选何种画面及原因；无增量可不设，不预设技术类别>`

| 原 Scene ID | Script / Research / 素材引用 | A/B 与观看目标 | 使用信息 ID | 对象与空间关系 | 场景策略与状态 | 媒体 / 占位 ID | 时间依据 | 上下场交接 |
|---|---|---|---|---|---|---|---|---|
| `<保留实际 ID>` | `<Anchor/选用条目/素材位置>` | `<阅读/比较/观察/理解关系>` | `<I01、I02；无则写无>` | `<可辨认对象及必要关系>` | `<顺序推进/切出返回/同场叠层；累积终态如适用>` | `<现有素材或 PH-ID>` | `<正式音频、对齐或临时预算引用>` | `<与前后场的进入/退出关系>` |

从当前 Script/Scene 索引生成，只列真实 Scene；不要求固定数量或 A/B 比例。Plan 不手填精确秒数，正式音频与对齐为时间真源，cues/Binding 保存解析值。

## 3. 上屏信息表

按完整理解单位建行，不按每句口播建行。`实际表达` 是本 Plan 唯一维护的实际上屏正文；原生素材文字或图形承载则写准确素材/图形引用及其传达的含义，不在 Scene 节拍表重抄。允许完整定义、引文和关键条件，不以缩字代替信息充分性。Plan 未采用的 Research 候选无需入表。

| 信息 ID / 来源 | 理解目标 | 实际表达 | 视觉职责 | 声画分工 | 揭示与阅读边界 |
|---|---|---|---|---|---|
| `I01 · <Anchor/Research/素材位置>` | `<需理解的对象、区别、条件、关系或结论>` | `<准确文字，或明确的素材原生信息/图形承载>` | `<辨认/定义/证据/条件/比较/导航/结论>` | `<画面保留什么，声音解释什么>` | `<不早于哪个 cue；在哪个状态必须可读>` |

## 4. Scene 节拍表

每场覆盖进入、阅读过程和离开；按实际变化填写，不套固定五阶段。证据页可只填一个持续阅读状态。舞台是角色、对象和空间关系，不提前摆满空卡片；正常阅读可以静止，已读信息可移动、归组或退出。新事实、条件和结果即使由图形表达也不能提前泄露；已知对象及中性空间可以提前组织。

### `<原 Scene ID>`

**场景策略与状态：** `<顺序推进 / 切出返回 / 同场叠层；只记录实际采用的保留/恢复状态及 B 接管区间>`

**不可破坏的关系：** `<仅写影响语义的对象对应、空间路径、遮挡或结果，不锁死 easing 与像素。>`

| 节拍 | 时间依据 / 区间 | 观看任务 | 屏幕状态与焦点 | 状态变化 | 语义与阅读保护 | 引用信息 ID |
|---|---|---|---|---|---|---|
| `<真实变化或持续阅读状态>` | `<音频 cue/停顿/事件间约束；无正式音频标 provisional>` | `<阅读/比较/观察/跟随变化/理解关系>` | `<已在场对象，当前焦点>` | `<进入/保留/移动/归组/退出；静止可写保留>` | `<尚不能揭示、不能遮挡或提前清除的内容>` | `<I01 等；无新信息可写无>` |

一场若需要多个节拍，继续在同一表添行；简单场景不为凑行数添加动作。一个共享 Three.js 舞台可以服务多个连续 Scene，不为文件分割破坏空间连续性。

## 5. 待补素材与占位

> 已有素材直接引用。这里仅列真实待补输入或需要明确构图的关键素材；同一个 icon 不必填一套长表。占位出现于 Plan、样段与完整占位 Draft；最终 Draft 的必需项必须就绪或经过明确变更移除。

辅助图形按 `.studio/spec/visual-design.md` 默认由制作者完成，不新增逐图审批或 Plan 字段。先用 `work component list --query <用途>` 发现，再读选中对象的精确引用、取用说明和示例；无可用对象则在 Windows 可编辑源制作，不把研究、候选或占位写成已接纳资源。

有独立身份或替换需求的素材保留稳定 ID，普通装饰原语不逐项登记。拟生成素材明确用途、数量、风格和 Asset Brief，方向批准一并授权这些素材，不逐张询问；未列用途或明显超量再确认。生成图不冒充真实界面/结果/数据，新提供方私有内容传输仍需明确授权。占位可以缺文件，不能缺设计。

**拟生成素材：** `<用途 / 数量 / 风格 / 对应 Asset Brief；不生成则写无>`

| 稳定素材/占位 ID | Scene / 对象位置 | 类别与表达职责 | 必须看见什么 / 完成判据 | 比例 / 裁切 / 留白 | 动作接口 / 起止状态 / 时间与声音要求 | 必需/可选 | 当前状态 / 取得方式 / 来源依据 |
|---|---|---|---|---|---|---|---|
| `<PH-ID>` | `<实际位置>` | `<截图证据/人物视频/图标/模型等>` | `<主体特征、内部内容及如何判断完成>` | `<实际要求；无需项留空>` | `<必要可动部分、变化结果、媒体源范围/时长/是否含声>` | `<必需/可选>` | `<占位或真实文件；复用/制作/取得途径；Research 引用>` |

**替换规则：** 用同一 ID 绑定真实文件，先保留对象、布局意图和主 cue；必要时局部调裁切/标注。替换若改变事实或已确认的主视觉方向，只复审受影响部分。图片文件 hash 不证明事实正确；真实截图与概念示意不能混同。

## 6. 音频卡点与实现约束

**主时间权威：** `<实际最终音频及既有对齐/剪辑映射；无则 provisional>`

**BGM / SFX：** `<默认不新增；已有或明确要求时写实际文件及其节拍引用>`

Scene 节拍表的时间依据引用实际 cue ID、Script Anchor 与词/字符位置；重复词须定位准确出现，必要提前/延后只记录意图。实际秒数/帧数由现有 cues/Binding 解析，不在 Plan、beats.json 与代码中维护第三套手填秒数。

**检查重点：** 依 `.studio/spec/visual-design.md` 检查完整进入、阅读、变化、交接和退出；文字在对应短句/核心词实际起点出现，不因相关词提前揭后文，不均分 Scene 时长或慢放简单动作。正常阅读可静止，无观看任务的等待不能靠装饰循环填补。工程任意 seek/回拖重建同样的状态。源 trim/take/rate 只确定映射，不改原对齐；无正式音频标 provisional。声音与命中前后帧在 Studio 核验，不导出视频。

## 7. 唯一参考 Scene

**Scene ID：** `<只填写一个当前 Plan 中的实际 Scene>`

**选它的原因：** `<能验证哪项主要解释方式、已有对象、素材入口和卡点。不强制同时展示全部技术。>`

**复用的已有成果：** `<M2/M3 的源文件/版本/预览引用>`

**本次参考实际展示：** `<真实画面、动画、音频及占位范围>`

**本次参考尚未证明：** `<未完成媒体、暂未对齐音频、其他 Scene 或新效果的技术风险>`

**官方 Studio / 准确预览版本：** `<生成后填入真实定位，不猜端口或路径>`

**方向批准：** `<未批准 / 用户已明确批准的原意与对应版本。不得自行填“已通过”。>`

一次批准覆盖本版 Plan 与这个 Scene 展示的制作方向，并授权按各 Scene 方案扩片；不代表全片已经验收。不再额外要求 Plan 单独批准、每 Scene 样段批准或每个 Work-local 对象先入库。

## 8. 后续交付与局部变更

| 交付 | 覆盖范围 | 允许的未就绪项 | 记录 |
|---|---|---|---|
| 完整占位 Draft | 全部 Scene、A/B 编排与实际已实现动作 | 计划内素材占位、明确的临时音频/已约定 proxy | `<准确版本与待补位置；不是 Final>` |
| 最终 Draft | 全片、最终音频、必需媒体及接受的效果 | 不保留必需素材占位；可选项可明确移除 | `<准确版本与最终验收状态>` |

准备度与准确 Draft 接受按 `.studio/workflow.md`；方向/占位不放行 Finalize。Draft 与 test-work 不导出视频，生产 Variant 独立 Finalize，归档另行标记。

**本次局部变更：** `<仅记录实际改动及影响范围，不复制历史所有审批。>`

**执行限制：** 不改 Script/Research 的已确认事实，不覆盖旧 Snapshot，不全库拆解，不因素材替换重写无关 Scene，不把单 Scene 的批准视为最终交付资格。
