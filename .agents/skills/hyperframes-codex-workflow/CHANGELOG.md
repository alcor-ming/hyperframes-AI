# Changelog

## 2.1.0 - 2026-08-12

- 下载视频新增 `dbs` 与 `verbatim` 两条 Script 路径，逐字模式保留原时间戳。
- 新增与 Script Revision 对齐的 `RESEARCH.md`，Animation Plan 必须引用当前 Research Revision。
- 固化丰富 GSAP 组件、统一入场、结束前 0.4 秒收束、progress/counter/pulse、多色进度与 Skill GitHub 介绍页规则。

## 2.0.0 - 2026-08-08

- 将 Skill 收敛为 `.studio/` 创作 Harness 的薄路由器。
- 保留两套 Template 与三套稳定 Profile，移除重复 Workflow、Prompt、Example、Schema 和模板文档。
- 从 v1 运行时移除图片生成、Prompt 检索、Taste Skill 和字幕流程。
- 作品生命周期改由根目录无依赖 `work` CLI 管理。

## 1.0.0

- 固化两套模板：口播增强与纯 HyperFrames。
- 删除字幕专用模板。
- 将 Animation Plan 定义为可持续讨论的轻量视觉 PRD。
- 用户可见计划收缩为 Plan Header、Scene Table、Optional Asset Brief 和少量待决策项。
- `section_map.json` 改为可选语义缓存。
- 纯 HyperFrames 支持计划确认后录音或 AI 配音。
- 增加可选 image-gen 资产流程。
- 增加静默 QA 与 HyperFrames 实现约束。
- 保持三套稳定 Profile 为外部单一来源。
