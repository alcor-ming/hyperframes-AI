# HyperFrames AI：逐 Scene 复用、缺口 Plan 与官方 Studio PRD

- 文档版本：v2.2
- 日期：2026-09-09
- 用途：交给 Codex 在现有仓库上迭代的需求与验收依据，不是另起项目的架构蓝图。
- 目标仓库：`alcor-ming/hyperframes-AI`
- 公开仓库审查基线：`fc65f01dbd74b25b6b19ae1019efe1f04a7bb526`。Codex 实施时以本地真实版本为准，先核对差异，不回退本地新能力。
- 主场景：AI 科普、Skill 介绍、工具机制讲解与工作流说明。
- 主要影响：`hyperframes_video`。不改造 `podcast_quote_image` 的内容、排版或审批流程。
- 范围声明：接通官方 Studio 与独立资产最短链路，不批量重建组件；保留 Theme / Background 解耦目标和 v2.1 内容质量，不增加日常必读文档。

## 0. 产品决策摘要

本轮不以“更多规则、更多组件、更多动画”为目标。核心交付是：**逐 Scene 筛选信息并匹配可用资产，只对真实设计缺口做轻量样段，数量可以为零；Draft 按各 Scene 方案装配，默认用官方 Studio 查看，由 Work 登记和接受准确版本。** 组件资产独立接纳，Work 使用固定副本；样段不升级为全片模板。

确定四项产品方向：

1. Plan 覆盖全片语义与逐 Scene 资产匹配；已有适配组件直接复用，组合或参数可解决时记录用法，只有真实设计缺口才做样段，可为零。同一缺口可覆盖明确列出的多个 Scene。
2. 采用“内容来源明确、文字 / 素材 / 动效各尽其责、稳定窗口支持阅读”的信息型动效设计，反 PPT 不等于少文字，也不等于每场强制复杂变形。
3. 完整叙事组件与底层效果分别复用，外部来源按兼容合同独立导入、验证和精确接纳；不随新增资产重发 Harness，也不等待历史整库清单齐备。
4. 反馈定位到 Scene、对象或时间区间；已经确认且未受影响的场景保持不变，避免局部意见触发整片重做。

官方 Studio 是默认制作与审阅入口，Work 只管理身份、版本、反馈和接受。Plan 批准前可为真实缺口创建 Work-local HTML/CSS 样段，主要动效先写方案；够判断即停止，不扩展为全片高保真预演。冻结版本只在隔离副本中打开，禁止覆盖历史快照或公共冻结资产。

## 1. 问题、依据与不确定性

### 1.1 原审查基线

用户确认当前返工严重、组件复用较少，文字 Plan 不能展示实际效果，主要内容需要同时保留丰富文字与具有解释力的动画。

公开基线的 Animation Plan 模板主要记录 Scene、Anchor、Hero State 和组件入场；工作流要求批准 Plan 后才能正式制作 HTML。[R1][R2] 这意味着可感知效果主要在后续 Draft 中被检验。

现有 Anti-PPT Skill 强调时间中的视觉事件，但同时要求始终采用含“不可逆变化”的场景公式，并把部分排版结构直接列为禁用模式。[R3] 这些规则需要改成针对信息表达的判断，不能机械用于每一类知识内容。

现有主 PRD 已确定 Profile / Background / Component 分工、画幅独立实现和历史 Work 冻结。[R4] 本轮保留这些方向，不以轻量化名义删减未来设计。

### 1.2 待验证的根因假设

| 假设 | 本轮如何验证 | 不能据此得出的结论 |
|---|---|---|
| 完整预演把太多制作前移，未必降低总成本 | 先复用适配资产，仅验证真实缺口，Draft 优先验证不确定镜头 | 不能保证动效返工归零或预先承诺 token 降幅 |
| 组件过于贴合某段原稿，换内容后无法适配 | 用不同长度、信息形态的真实输入试装候选组件 | 不能据此批量拆掉全部组件 |
| 组件检索只看名称或效果，忽略承载能力 | 在当前真实文字、时长、画幅中预演匹配结果 | 不能靠强制复用率解决适配问题 |
| 隐喻、文字、镜头同时争夺注意力 | 检查真实速度下的阅读与焦点迁移 | 不能简单得出“必须删字”或“必须减少所有动画” |
| 修改范围不清，已确认片段被重新生成 | 对局部改稿记录受影响 Scene，并检查邻接交接 | 不能把局部反馈变成全局禁止规则 |

没有读取本机私有 Work，也未执行本机渲染。本轮的效果改善是需要验证的产品假设，不承诺固定比例的时间节省。

v2.0 修订输入包含用户对 Work 011 的调查报告：Plan 已承担长片布局、动效、媒体同步和裁切，但无法证明总 token 减少。v2.2 再针对 Work 012 报告中的自制预览默认入口、样段全片传播和关系压平修订。这些是用户提供的调查依据，不冒充本文独立审计。存量 Work 先核对真实状态，只修受影响 Scene 与必要交接，不删除已完成内容、旧预览和接受记录，也不另起 Work 重做；本文不声称生产 Work 已迁移。

## 2. 目标、非目标与保留边界

### 2.1 目标

- 在 Plan 确认时，逐 Scene 的信息关系与精确资产选择明确；仅有真实缺口时用实际文字样段验证布局、配色和字体，区分已展示范围与未验证动作。
- Draft 逐 Scene 复用匹配资产、已确认内容、tokens、样式和有用代码，再补动作与媒体；不要求预先存在完整工程，也不把单个样段布局全片复制。
- 增加 GSAP 的表达能力：除入场外，支持重点迁移、关系呈现、对象转化、证据放大与连续交接。
- 保留应出现的术语、论点、条件、操作步骤、例子与边界，不用空泛视觉替换实质信息。
- 常用 Three.js 效果可以以有限参数接入不同内容，可靠拖动、回放和渲染。
- 新增局部修改能力与少量结果记录，而不是新增复杂任务系统。

### 2.2 非目标

不新增第三条工作流；不开发通用视频编辑器、节点编辑器、在线 Registry、向量检索系统、自动审美评分器或多代理串行审查系统。不以本轮为由批量创建几十种组件或 Three.js 特效。不建设新渲染引擎，不强制迁移到 React、WebGPU 或其他技术栈。不把分段编码缓存作为局部改稿的前提。

### 2.3 保留的约束

保留 Work / Variant、外部私有 WorkStore、稳定 Scene ID 与 Script Anchor、Draft 快照、Final 历史和可恢复归档。口播改写仍遵守已有授权；不生成或烧录传统底部字幕。画面内的解释性文字不是底部字幕，不受此排除项限制。

保留 `talking_head` 与 `pure_hyperframes` 两模板。保留组件的画幅独立设计，不为追求通用率把多个画幅退化成自动缩放。原 42 项 Gallery 的整组完成约定作为历史批次 backlog 保留，但不阻塞单个兼容资产接纳或当前 Work；试验性接线不冒充整库迁移完成。

公共库只保存可公开的实现、合同、必要来源信息及脱敏样例。真实作品的文案、截图、素材、预演、反馈和成片仍在 WorkStore。安装第三方效果前核对许可和依赖来源；“公开可访问”不等于“可以任意打包”。

## 3. 工作流：先匹配资产，只验证真实缺口

### 3.1 新工作流

```text
内容输入 / 已有文案
  → Script 的口播 / Scene 索引与内容缺口明确
  → Research 按缺口保存资料依据和采用信息；可以无新增研究
  → 按 Scene 筛选信息、匹配可用资产、记录精确引用和内容绑定
  → Animation Plan：全片结构、逐场表达、主要动效方案与素材位置
  → 只有真实设计缺口才做实际文字样段；数量可为零
  → 用户一次确认 Plan：可见设计与实施方向
  → Draft 优先实现最不确定的真实镜头，再补齐其余 Scene、动作和媒体
  → 官方 Studio 查看、定位和反馈，Work 登记准确版本并接受 Draft
  → 正式配音接入或同步修正、Final QA、正式渲染、归档
```

已有正式音频时沿用其时长信息，没有时允许估算；Plan 不为布局查看强制录音、ASR、长音频同步或视频导出。内容理解与事实核查所必需的媒体仍按原流程处理。`talking_head` 正式解释层继续以实际媒体对齐，`pure_hyperframes` 保留先视觉、后正式配音。

预演是原 Plan 的可视化表达，不是 Plan 后面新增的 Motion Plan 审批。Draft 确认检查整片完成度和实际差异，不重复询问已确认的创作方向。

### 3.2 方案覆盖全片，样段只覆盖缺口

全片保留 Scene / Anchor、内容或素材引用、表达目的、动态意图、资产选择与待定项，不复制完整信息稿或表现标签表。先区分已适配、组合或参数可解决、只待库级验证或接线、真正设计缺口；前三者不自动生成新审批样段。真实缺口按问题分组，一个样段可覆盖多个明确适配的 Scene，也可以完全没有新样段，不设“至少一个”或“每场一个”。

全片颜色、字体和基本文字层级可共享；样段的信息分组、局部布局与动作默认只覆盖展示 Scene，其他 Scene 要在 Plan 明确引用和说明适配差异。尚未实现的路径、分支、反馈或 Three 效果仅记录实施方向，不冒充已看过效果。

保真度按阶段分配：

| 内容 | Plan 阶段要求 | 可留到 Draft 的内容 |
|---|---|---|
| 内容、配色与字体 | 已适配资产检查当前文字承载；真实缺口样段采用实际内容、颜色、字体、字号、行距和层级 | 实际绑定后的越界与真实速度阅读 QA |
| 布局 | 目标画幅、内容区域、媒体槽位和视觉重心 | 全片实现与动态重排 |
| GSAP | 对象、关系、起止状态与阅读安排 | 时间线、精确 easing、逐帧时序与复杂变化 |
| Three.js / 高成本效果 | 静态示意或已有参考，说明空间表现及必要替代 | shader、模型、粒子、灯光、镜头与性能调试 |
| 媒体 | 有语义占位；已有且影响布局的真实图片可复用 | 获取、处理、裁切、同步及替换 |
| 时间与衔接 | 只记规划预算或媒体依据、阅读顺序和节奏意图 | `section_map.json` 实测时间、工程实际出现 / 持续、音画同步与重定时 |

无风险的既有设计不额外生成多个方向；确实影响整体风格的新机制，可少量并列比较。默认给一个推荐方案，不给每个 Scene 都做三套完整设计。

### 3.3 样段查看与停止条件

样段设计仍是普通 HTML/CSS，必要时用极少 JavaScript 切换静态状态。默认由锁定官方 Studio 查看，只增加本机版本需要的最小 composition、画幅和展示时长，不另写一份 Studio 专用设计；展示时长不冒充口播时长。不制作新 GSAP/Three 动作，不加载真实视频或音频，不初始化全片 Scene、同步时间线或 MP4 渲染。

样段用途、Scene 与未实现动作留在 Plan、CLI 和运行元数据，不绘制成观众画面角标。Work 返回经实际启动核对的 Studio 工程 URL，不将源码 `index.html` 路径冒充预览地址，不硬编码端口。本机锁定版本不支持时明确说明；临时替代须由用户选择，不静默回退自制审阅页。完整 executable 预演仍为明确选择的高级路径。

每场都有匹配方案、引用或明确缺口，必要样段已能判断设计时就提交 Plan；没有缺口则引用已验收资产 Preview，不重新生成同一演示。未制作 Scene 保留简短方案，缺内容标缺段，不为 Studio 时间线临时补内容或扩成整片。已认可视觉直接引用，不重复风格问卷、组件设计或逐帧测试。

后续修改只看受影响片段。高级多 Scene 播放仍不得让所有 Three.js 缩略图同时常驻运行，不建设新编辑器。

### 3.4 低成本预演不等于低保真设计

简易指范围与实现简单，不指审美粗糙。样段使用真实画幅、颜色、字体、换行和来源明确的实际内容；需要阅读的文字实际排入版面，素材已经清楚呈现的信息不强制重复覆盖。不以灰色线框、Lorem ipsum、几个口号或无限缩字冒充已验证设计。

媒体占位注明用途、对象、比例与裁切意图，必要时写亮暗和方向假设。不使用假 URL、失效 src 或空文件；占位不触发下载、probe、转码、Range 或全媒体哈希，实际用到的本地资源仍检查。事实不能靠占位证明。主要动作先说明映射、阅读位置和结果；新颖或昂贵时补必要替代，不要求每场完整技术表。

一个核心镜头确实决定布局、文字位置与颜色感知，或技术可行性影响内容时，先看已有实现、代表帧和参考；仍不能决定才给替代或最小问题样段。默认在 Draft 内最先验证，不新增强制审批，不因局部试做扩大为整片预演。

预演需要真实制作投入，这是把不确定性前移，不是免费消除它。成效必须以“从开始到最终接受的总投入”衡量，不能只把返工从 Draft 挪到 Plan 后就宣称成功。

### 3.5 同源复用与审批记录

`ANIMATION_PLAN.md` 继续是视觉意图、Scene 选择和用户决策真源；场景源码与 Binding 是可执行真源。Studio 可写当前工程，但不成为第二份独立文案真源；编辑字段与当前选稿不一致时同步原真源或暂停该字段再生成，不默默覆盖手工修正。

Plan 确认关联全片方案、精确资产与必要样段版本，区分全片视觉身份、局部适用 Scene 和同意实施但未验证的动作、节奏、媒体；零样段同样可以确认。不另造审批库。Plan 接受不能写入 `accepted_preview` 或放行 Final；旧记录按原语义读取，不重新解释为静态 Plan。

Draft 复用已确认的文字、tokens、CSS、内容绑定、稳定 Scene ID 与有用代码，允许必要结构改造，补齐其余 Scene 和动作；不要求 Plan 先具备全部 composition、vendor 或生产时间线。在已选方向内实现未验证动效不重复批准，主构图、视觉含义或内容承诺实质改变时仅确认受影响部分。修 bug 不作为新审批。

生成器按 Scene 的 Plan / Binding 保留角色、连接、条件、分支去向、反馈与结果；不将 `participants`、`branch`、`feedback` 全部压成统一字符串行。未支持表达明确返回该 Scene 的实现缺口，不静默套通用卡片。可共享节点、连线和入场函数，但实际关系必须在可见状态或动作中成立。

## 4. Animation Plan 的最小内容调整

不新增独立的 `MOTION_PLAN.md`、`SCENE_SEMANTICS.md` 或手工维护的第二份 Scene 数据库。增加的信息仍归入现有 Plan，并按需生成查看数据。

| 项目 | 内容要求 |
|---|---|
| 作品目标 | 受众、看完应理解什么、必须保留的信息、不能采用的表达 |
| Scene 与 Anchor | 稳定 ID 与逐字口播引用；不复制整篇口播 |
| 内容 / 素材引用 | 独立信息引用 Research 采用条目，原生文字引用素材；表现标签留在工程，不维护全文清单 |
| 表达职责 | 本段由文字、素材、操作或动效表达什么，是否为示意；不逐字重复素材已清楚呈现的信息 |
| 动态与阅读状态 | 主要状态变化、信息焦点、可读稳定区间、必要交接 |
| 资产与复用 | 组件、效果或 Work-local custom 的真实引用；不写虚假的“已复用” |
| 样段范围与证据 | 可为零；如有则记录具体缺口、版本、适用 Scene、全片可共享身份与仅局部生效的布局；未验证动态/媒体单列 |

样段片段中需要阅读的文字进入具体排版，其余 Scene 保留内容或素材引用；不复制整篇稿件到多处，也不为轻量样段新建 schema、组件编译器或全片投影框架。

主模型可在样段 / Draft 中直接调整换行、分组、顺序和表现标签；等义精简独立信息时原位更新 Research 采用条目及呈现，不重新联网或增加审批。新增数据、比较或因果只补查相关条目；删除核心信息或改变事实、观点、因果及已确认叙事时才提出局部取舍。Revision 继续留痕，但先按实际引用范围同步，不自动刷新全片或改写旧接受快照。

Scene 仍是唯一强制层级。只有复杂场景需要时才细分 Beat；不要求每一场都填固定数量的状态、动词、拒绝候选或证明条目。

## 5. 文字丰富与反 PPT：信息型动效规范

### 5.1 三条设计原则

**信息先完整，注意力再编排，动画服务于解释。** 三者不是先写一屏文字、再额外加粒子的顺序，而是共同设计时间中的表达。

“丰富文字”指一条视频实际传递了足够具体的信息，不意味着所有段落必须同时出现在一帧。“反 PPT”指画面不只是固定页面中逐项进入的信息块，不意味着禁止文字、列表、对比表、截图、卡片或稳定阅读状态。

视觉目标不是把动画退回朴素淡入。允许有质感的动态构图、空间层次、节奏对比和跨对象交接；丰富动效与稳定阅读应在时间、区域和焦点上协调，而不是互相取消。

不能用删掉限制条件、隐藏必要步骤、把完整解释改成几个口号来换取画面清爽。用户明确要求的内容必须被正确表达，但可以由文字、素材、操作或动效承担；等义归纳必须保留结论与重要限定，核心取舍仍需确认。

### 5.2 文字、动效与证据的职责

| 表达渠道 | 主要职责 | 不应承担的职责 |
|---|---|---|
| 标题、段落、步骤、标注 | 精确描述术语、动作、结论、条件与限制 | 每一行都持续运动来制造动感 |
| GSAP / SVG / DOM 动效 | 引导焦点，揭示关系，保持对象连续性，演示状态变化 | 用无意义入场替代解释 |
| Three.js | 在确有价值时表达空间层级、聚合分布、路径、遮挡或镜头变化 | 接管所有正文、以旋转粒子代表所有 AI 机制 |
| 截图、录屏、实际输出 | 为操作、结果和差异提供可核验依据 | 以程序模拟界面冒充真实产品能力 |

这些是职责区分，不要求每个 Scene 固定采用三层画面或同一种左右分栏。

### 5.3 先分配信息，再设计运动

在现有 Scene 中区分三类内容：主命题、支撑解释、条件或证据。不是所有场景都需要三类齐全；有需要就具体呈现，不能为了模板凑齐。

继承 v2.1：内部制作说明不进入观众正文；无信息增量的重复删除，不迁到脚注、口播或下一状态复活。可选解释为空就不生成，不用模板空话补 Slot；真实来源、原文对象、必要导航与条件保留，选稿只维护一个真源。

优先采取以下方法保留信息密度：

- 按含义分组显现，而不是逐字、逐标点播放整段正文。
- 当前解释清晰可读，已解释内容可以留作稳定上下文；下一组出现时明确转移焦点。
- 通过替换、重组、局部放大或展开显示细节，而不是不断往同一页面堆卡片。
- 术语、对象和对应图形保持一致的标注与关联；文字不因隐喻变化丢失指向。
- 实际操作展示可以短暂停留并给出完整说明；“暂停以便理解”不构成反 PPT 失败。

信息太多时，先调整分组、场景内部时序与版面，再判断时长是否足够。不能默认无限缩小字体，也不能为了保持动画速度省掉必要内容。确实无法在既定音频中容纳时，只针对该段提出取舍。

### 5.4 阅读窗口与注意力平衡

每一组需要阅读的文字都应有可辨识的稳定阅读区间。该区间内正文不应持续漂移、缩放、乱码变换或被景深模糊；镜头与特效不能不断把视线拉离正文。

文字进入新的复杂解释时，降低其他区域的运动强度；需要观察操作或空间变化时，相关说明可以保持稳定，用小范围强调同步解释。轻微环境运动可以继续，不要求整个画面静止。

不设置所有 Scene 通用的字数、字体大小、文字占比、动画占比或固定停留时长。评估依据是当前画幅、真实字数、受众、媒体节奏、排版和实际显示尺寸。代码可报告遮挡、越界、短暂显示等问题，不能把经验阈值伪装成自动审美结论。

原模板中的“全部主动作在结尾前 0.4 秒结束”不再作为全局阅读规则。结尾短暂停顿不能替代贯穿场景的阅读设计；可保留为特定组件的既有动作合同，使用时仍检查是否足够承载当前内容。[R1]

### 5.5 动效隐喻必须有信息映射

每个关键隐喻需要说明：画面中的对象代表什么、动作对应何种关系、观众应得出什么结论。用一句话或简短 Scene 注记即可，不强制新增结构化文件。

例如，“资料被筛选”可以表现为候选项保留或排除，文字同时解释筛选依据；不能只让粒子进入一个发光球，就声称已经解释了检索、推理或事实核查。

对并列功能保留并列关系，对比较保留比较关系；只有确有依据时才表现为因果或必经顺序。不把工具串联的视觉隐喻误写成其真实产品能力。概念示意、模拟操作、真实录屏应可区分。

数字、百分比、速度、成功状态和排名必须有实际依据，或者清楚标记为演示值。取消对所有机制套用 `0 → 100%` 进度条的倾向；无法定义进度时，使用阶段、选中、传递或状态变化，不制造精确度。

### 5.6 正反示例

| 内容 | 可采用的表达 | 应避免的表达 |
|---|---|---|
| 一个 Skill 如何处理输入 | 输入对象沿真实步骤变化，文字逐步解释输入、动作、输出与限制 | 标题下面三张卡片依次弹出，内容与运动无关 |
| 几个 Skill 的适用场景 | 保留对比项，用焦点迁移和局部展开展示差别 | 为“连续感”把独立工具硬接成工作流 |
| 资料筛选或聚合 | 候选项有可追踪身份，筛选原因与结果同步标注 | 粒子随机聚散，观众看不出什么被保留 |
| 真实操作证据 | 放大实际相关区域，稳定显示注释，结束后收回上下文 | 整页截图快速缩放，文字既看不见也没时间读 |
| 机制的局限 | 正文清晰保留条件，相关对象在同一上下文中标出限制 | 只显示一个醒目的结论，把限制隐藏或口号化 |

### 5.7 一个 Scene 的示意样例

以下仅说明设计方式，不替用户预定组件。主题为“一个已核实的素材整理 Skill 如何工作”；实际能力、文案与结果必须以当前 Script / Research 为准。

| 示意区间 | 画面行为 | 可见文字与阅读安排 |
|---|---|---|
| 0–4 秒 | 显示输入材料，建立一个可追踪的材料对象 | 标题与完整输入条件稳定出现，如“输入：正文、来源地址与已有标签” |
| 4–10 秒 | 保留材料身份，突出正在处理的字段；GSAP 将相关信息局部展开 | 显示实际处理规则，而不是只有“智能整理”四个字；已讲清的标题留作上下文 |
| 10–16 秒 | 合适时由 Three.js 或 SVG 表现分组、归位，说明文字不随空间对象旋转 | 标注输出结构与来源保留方式；动作结束前后都能对应到原材料 |
| 16–22 秒 | 输出进入稳定阅读状态，并衔接下一个例子 | 显示必须保留的限制，例如人工核对要求或适用范围，不以特效替代 |

这里的时间只是示意，不成为模板常量。一个短 Scene 可以更简单，一个复杂 Scene 可以有更多信息阶段；没有必要时不使用 Three.js。

## 6. GSAP 能力升级方向

### 6.1 从“入场集合”升级为“可复用解释动作”

本轮优先支持下列能力类别，不要求一次性全部封装为公共 API：

| 能力 | 适合解决的问题 | 内容保护要求 |
|---|---|---|
| 焦点迁移、重点标注 | 让读者知道此刻正在解释哪一部分 | 其他必要文字仍可识别，不全部模糊消失 |
| 对象连续转移与路径运动 | 输入到输出、数据传递、材料归位 | 保留对象身份，不暗示不存在的因果 |
| 布局重组与比较展开 | 在相同空间承载更多解释或切换比较维度 | 重组后完整内容可读，不把整个正文持续做 FLIP |
| 证据放大、局部揭示 | 解释真实截图、代码或结果 | 关键证据不能在停留不足时快速闪过 |
| 关键词与图形关联 | 解释术语、条件与对象关系 | 关键词是完整解释的导航，不是正文替代品 |
| 有意义的跨 Scene 交接 | 把上一段结果带入下一段问题 | 不把所有内容强行串成同一套视觉故事 |

允许丰富的 easing、适当的预备动作、跟随和收束，但按内容需要选择，不设置“每场至少几个 tween”“每个组件都必须独立入场”的指标。

### 6.2 复用方式与技术边界

优先把已验证的动作封装为返回有限 timeline 的小函数，或在适合时使用 `gsap.registerEffect()`，而不是新建通用动画引擎。GSAP 官方支持注册可复用效果和基于状态的 Flip 过渡；是否引入插件应按本项目锁定版本、部署方式和实际场景决定。[T1][T2]

用于普通交互网页的动画示例不能原样当成视频效果。正式合成的状态必须能被直接定位，不依赖用户滚动、鼠标悬停、滚轮或实时事件。

遵守当前 HyperFrames 对 timeline 注册、同步构建和资源就绪的实际要求；异步资源通过既有就绪机制处理，不在抓帧过程中才临时拼装时间线。

同一个属性在同一时间由一个清晰的控制者拥有。组件内部动作与 Scene 编排通过明确的开始时间、局部时间或参数衔接，不叠加多个互相覆盖的 timeline。

涉及 Flip、动态排版或文字拆分时，在资源和布局就绪后确定必要状态，再构建可 seek 的动画；不能在某次播放回调里才修改 DOM，使随机跳转时缺失正文或状态。GSAP 的 `seek()` 默认抑制回调，因此不能把渲染关键更新仅放在 `onUpdate`、`onComplete` 等回调中并假定所有抓帧都会执行。[T3]

## 7. Three.js 常用效果复用

### 7.1 定位：效果部件，不是新的一套场景系统

Three.js 效果是可由 Component 或 Background 组合使用的实现模块。它不能反向拥有整条视频的叙事、正文、Profile 或全局时间轴。

由承载者决定效果职责：承担叙事、处理关系或主体空间变化时归属 Component；仅提供环境时归属 Background。不得以“效果层”为由绕过现有前景/背景边界，增加一个到处改动场景的全局管理器。

文字默认继续由 HTML / SVG 排版，Three.js 负责其擅长的空间表现。必要的短标签可以与三维对象绑定，但长段中文不默认转成旋转的 3D 字或纹理正文。

### 7.2 候选方向

以下是按真实案例选择的方向，不是本轮必须完成的效果清单：

| 效果方向 | 适合的解释目的 | 边界 |
|---|---|---|
| 可控粒子或点集汇聚、分散、筛选 | 表现集合、分组或候选选择 | 粒子需要被标注或与信息对象对应，不能只是装饰 |
| 节点、路径与信号传递 | 表现资料流向、分支或协作关系 | 不把示意网络当成真实神经网络内部机制 |
| 层叠展开、空间拆解 | 表现不同层次、组成关系与输入输出 | 必须保留层名和对应说明 |
| 对象变形与结果归位 | 表现结构转化与组织结果 | 形变本身不能替代转化规则说明 |
| 受控镜头、聚焦与浅景深 | 引导查看对象的局部和整体 | 正文不跟随镜头丢失，不靠景深掩盖必要信息 |

首轮从真实 Work 选最有价值的一到两个效果验证即可。没有真实素材时，可用脱敏替代用例验证技术链路，但应明确它不是已证实的真实生产复用。

### 7.3 最小效果合同

只定义接入职责，不要求立即建立复杂 schema 或插件平台：

- **初始化**：接收宿主、输出尺寸、主题参数、必要资源与固定 seed；状态不依赖另一份 Work。
- **时间求值**：接收可确定的 Scene 局部时间，直接得到这一帧的对象、镜头和 shader 状态。
- **有限配置**：只暴露真实需要变化的对象数量、路径、分组、密度、色彩映射、动作区间等，不为所有可能性添加参数。
- **生命周期**：有明确的载入就绪、显示/隐藏和释放行为；销毁时清理自身资源和监听器，不能释放其他组件共享的资源。
- **依赖与归档**：效果代码、必要 shader、模型、纹理及版本引用进入既有 Work 快照和依赖闭包，不依赖运行时去网络临时下载。

画幅适配由宿主的已设计实现负责；底层数学或绘制逻辑可以共享，但不因此取消 `4x3` 与 `16x9` 的独立构图。

### 7.4 与 HyperFrames 的接入

上游现有 Three.js 适配说明提供 `hf-seek` 和 `window.__hfThreeTime` 的逐帧驱动方式。[T4] Codex 应先检查本机安装版本和真实适配器，再决定接入位置；上游存在不等于本地安装已经支持。

优先复用既有适配器，统一 DOM / GSAP 与 Three.js 的帧时间。要验证全局时间到 Scene 局部时间的转换、偏移、暂停、重定时，以及同一帧中参数更新和 WebGL 绘制的顺序，不能只注册一个事件就宣称混合渲染完成。

禁止把 `Date.now()`、实时 delta、独立的 `requestAnimationFrame()` 或 `setAnimationLoop()` 当作成片动作的时间权威。可使用固定 seed 生成初始数据，再从时间计算状态；不能依赖“先播放上一帧”才能得到正确下一帧。

依赖历史帧的物理模拟、反馈 shader、拖尾或累积采样不进入首轮，除非能重建任意时刻的结果或提前烘焙。固定初始化、资源就绪和有限时长；不用无限 repeat 填充场景。

主库与 addons 的版本保持一致并固定在项目依赖中。渲染尺寸、像素比和必要色彩配置由工程控制，不随打开预览的屏幕设备随意变化。Three.js 提供显式绘制和资源释放 API，具体组合仍须在本项目实际环境验证。[T5]

### 7.5 不可用时的处理

Three.js 出错不能阻止不依赖它的普通 GSAP 工作。该效果不得悄悄变成静态图还登记为原效果通过。

若已批准方案包含明确且等价的降级表现，可以使用并记录实际输出；否则只报告受影响 Scene，并提出具体替代。主视觉或表达发生实质变化时需确认该部分，不重启整个作品审批。

## 8. 独立资产与有效复用

### 8.1 分开理解三种复用

| 类型 | 真正复用的内容 | 适用方式 |
|---|---|---|
| 完整叙事组件 | 信息结构、Slot 合同、布局、动作与交接 | 内容形态确实匹配时直接使用 |
| 动作与效果模块 | GSAP 动作、Three.js 绘制与时间行为 | 由不同组件或 Work-local 场景组合 |
| 案例与设计经验 | 如何表达某一信息关系 | 用于参考，不登记为代码复用 |

复制一份案例再重写内部代码，不等于调用了同一组件。一个效果被很多不相关场景强行使用，也不代表有效复用。

### 8.2 面向 AI 科普与 Skill 介绍的选型方向

优先从“需要解释什么关系”发现候选，例如输入—处理—输出、筛选、比较、分支协作、证据引用、信息回写和结果复用；不要只按“玻璃卡片”“发光粒子”或某个 Skill 名称分类。

匹配时同时看当前画幅、文字长度与信息形态、素材形态、可用时间、进出场关系和不适用条件。Profile 不应成为语义组件是否可用的唯一依据；主题兼容沿用现有主 PRD。[R4]

已有精确版本已接纳、合同兼容且内容适配时直接复用；当前文字绑定后的越界与渲染检查属于普通制作 QA，不重复资产设计审批。候选仅待依赖验证或接线时先做一次库级验证，不让每个 Work 重做样段。默认给最合适候选，不要求每场冗长拒绝清单。

### 8.3 保持可改，但不破坏冻结

优先通过 Slot、参数和组合适配。若一个长解释超出组件能力，应选择更合适的布局、组合或 Work-local custom，不为了塞进组件删掉关键信息。

需要改内部实现时，从原组件明确派生到 Work-local custom，记录来源，不原地改写冻结 vendor，也不强迫每次试验先发布一个新公共版本。新公共 Release 继续走已有设计与验收边界。

跨案例反复出现的稳定结构再抽取公共能力。可将“两次不同真实用途仍能不改内部实现”为优先沉淀信号，但不是所有开发都必须等待的硬门槛。测试样例中的两次绑定只证明可配置，不证明生产中已经复用成功。

### 8.4 本轮不交付什么

不固定组件总数、命名、分层数量或强制复用比例。不要求用户先完成整库设计才能使用新预演流程。不把已有未来组件清单删掉，不把用户在本地找出的案例偷偷替换成 Agent 自选的一套模板。

具体组件设计仍由用户的真实案例驱动；保留完整 Theme / Background 解耦和原 42 项 Gallery backlog，不宣称本轮完成整库迁移或所有资产类型的独立安装。

### 8.5 独立资产最短链路

Harness 提供合同、生命周期和验证，外部配置指定 AssetStore 与已存在的资产来源。包的元数据驱动发现，不要求新增资产时修改 Python 常量、`capabilities.yaml` 枚举、全局 Skill 或主 PRD。当前优先接通 Component / 同合同效果，其他类型必须有实际合同与接线才可登记为可用。

外部包先导入隔离候选，按精确身份、版本、包摘要、Fixture 与所需运行环境记录接纳；`migration-ready` 不等于已批准。接纳记录不修改 hash 覆盖的实现，不能把全部状态字符串替换为 approved。官方 Registry snippet 只有经过明确选中、最小接线和本项目验证后才可接纳，不自动上报 Work 内容或缺口。

Work 沿用 vendor / Binding / `COMPONENT_LOCK.json` 固定实际包及依赖，不读取 WSL 活跃源码或 AssetStore latest 链接。同身份、同版本不同内容拒绝覆盖或使用；库更新不热改旧 Work，只核对当前使用的依赖。旧 Work 在 AssetStore 离线时仍从自身闭合副本预览和渲染，兼容新资产无需重发 Harness。

## 9. 局部修改与重定时

### 9.1 反馈的最小绑定

反馈至少能定位到当前预演/Draft 版本及 Scene；有需要时进一步定位时间区间或对象。优先使用 Studio 的稳定元素 ID、源文件、composition 路径和时间；上下文查询绑定准确工程与实际端口，缺选择时不猜对象，也不默认读取整页 DOM 和全套样式。

例如：“S04，18.2–23.5 秒，保留这两段文字，改掉卡片轮流入场，资料筛选过程要看得见。”这授权的是该段视觉调整，不是重写全文或禁用整个项目的卡片。

### 9.2 修改传播规则

| 修改 | 处理范围 | 是否重新确认 |
|---|---|---|
| easing、换行、同语义强调、边距修复 | 当前 Scene，必要时检查交接 | 不新增审批，展示实际差异即可 |
| 调整某个效果的局部参数，不改变主视觉与含义 | 当前效果及对应 Scene | 不重复确认整片 |
| 改变隐喻、主构图、视觉目标或新换 Three.js 主效果 | 当前 Scene 与受影响交接 | 只确认变更部分，更新现有 Plan |
| 更换配音但语义结构不变 | 相关时间映射、阅读窗口与交接 | 不重新选择全部组件或视觉方向 |
| 改变口播事实、信息目标或整体结构 | 对应 Script / Research / Scene；确有全局影响才扩大 | 沿用既有内容确认边界 |
| 升级公共效果或组件 | 默认不传播到已冻结 Work | 用户明确要求采用时才更新并检验差异 |

局部改稿不等于已经实现视频分段缓存。即使最终仍整片重新编码，也应只修改必要源码、只重新讨论实际变化的设计。

### 9.3 正式配音接入

不默认把全场做统一 `timeScale` 拉伸。应区分表达动作、稳定阅读和交接：优先调整可变停留，再调整可变动作，检查剩余空间是否足以阅读。

如果当前组件只支持整体缩放，也应检查其动作速度与文字阅读是否仍合理。确实超出能力时，只处理该 Scene；不得静默截断配音、删掉必要上屏内容或把已经确认的设计全部作废。

精确到帧的时间映射由现有渲染与媒体对齐机制决定，不能把本 PRD 的示意秒数变成硬编码节奏。

## 10. 最小实现落点与现有规则修订

以下是实施位置建议，不是必须逐项新建文件的清单。

| 位置 | 本轮修改 |
|---|---|
| `AGENTS.md` | 逐 Scene 匹配、缺口样段可零、Studio 默认、局部范围与既有权限 |
| `.studio/workflow.md` | 全片方案与逐场资产选择、真实缺口才样段、Plan / Draft 分离、Draft 优先实现风险镜头 |
| `.studio/templates/ANIMATION_PLAN.template.md` | 增加内容 / 素材引用、表达职责、动态 / 阅读状态与样段范围；去除全局统一进度条和统一 0.4 秒收尾规则 |
| `.studio/spec/creative.md` | 明确 Script / Research / Plan / 工程内容归属、等义适配、真实证据与阅读保护 |
| `.studio/spec/hyperframes.md` | 增加 GSAP / Three.js 同一帧驱动、资源就绪、随机 seek、渲染一致性与必要 QA |
| 视频 Router Skill | 已适配资产引用路径与缺口 layout 路径分开；默认官方 Studio，局部样段不全片传播 |
| `hyperframes-anti-ppt/SKILL.md` | 从统一场景公式改为信息型动效指导；允许对比、列表、截图、稳定阅读；只指出具体表达问题 |
| `.studio/work.py` 及相关帮助模块 | 解析准确工程并薄路由到锁定官方 Studio，隔离冻结版本，保留版本、反馈和接受 |
| `.studio/component_harness.py` 等 | 外部来源、候选验证、精确接纳、元数据发现与固定副本；不全库重构 |
| 实际 Scene 生成入口 | 读取 Plan / Binding 的信息关系与资产引用，拒绝未支持表达，不压平语义字段 |
| `tests/` | 零样段、范围不扩散、Studio 身份和隔离、语义保真、独立资产及旧 Work 回归 |
| `docs/PRD/` | 将本 PRD 作为专项升级需求并加入索引；保留主 PRD 中不属于本轮的设计 |

### 10.1 Plan 预演的代码边界

现有正式 Draft 的批准检查继续有效。Plan 可在未获批时创建，仍要求 Script 与按内容缺口整理的 Research 就绪。无新样段时只记录逐 Scene 匹配和引用；有缺口时 layout 仅检查方案、样段和实际资源。Studio 需要锁定官方 CLI，但不因此要求 Three、FFmpeg、正式音频、全片时间线或完整工程。占位不是运行依赖；Draft / Final 必需媒体、效果与来源检查不放松，可选空 Slot 不算缺失。

如果候选组件安装入口当前只允许 Plan 批准后执行，应补足“预演用途的受限 Work-local 绑定”，而不是把正式生产的批准检查整体删除。公共 Release 验证、路径约束和冻结资产不变性仍有效；预演也不能把未正式可用的资产冒充库中已批准组件。

零样段使用 `preview register --purpose plan --kind reference`；真实缺口沿用 `--kind layout --sample-dir <Variant内目录> --scene S01`，Scene 可重复。`preview open current` 打开当前工程，指定登记版本则打开隔离副本；`preview accept` 仍管理接受。旧记录和省略 kind 的调用按原 executable 语义兼容，完整预演需明确选择，不是默认制作范围。

### 10.2 快照与渲染

Plan 的确认快照与 Accepted Draft 复用基础机制，但按用途校验。layout 冻结 `SCRIPT.md`、`RESEARCH.md`、`ANIMATION_PLAN.md` 作为来源输入，以及实际样段、样式和使用资源；未实现效果、未取得媒体和完整工程不进入必需依赖。局部等义修改只可在冻结来源齐全、口播与 Anchor 不变、Plan 视觉正文不变时，由主模型声明范围并通过冻结 / 当前输入、Revision 与工程哈希检查；旧记录缺冻结来源时保留原流程，不自动宣称兼容。兼容只沿用 Accepted Plan 进入新 Draft，来源内容或 Revision 变化的 Accepted Draft 仍须重新注册与接受；Plan 的纯状态和 `visual_plan` 元数据变化例外。Draft / Final 完整效果、shader、媒体快照机制保持，不设计第二套锁文件系统。

Studio 会写工程，所以 Accepted Snapshot、冻结 vendor 和外部已发布资产目录不能直接作为其可编辑根目录。登记版本导出真实复制的隔离副本，不用 hardlink/symlink；副本编辑不自动成为当前 Work 或批准结果。Studio 改动当前源码后，旧 MP4、截图和 QA 不再被关联为当前源码证据，按既有机制重新生成受影响输出。

Final 继续从 Accepted Draft 派生，记录实际使用的源码与媒体版本、渲染参数和输出摘要；允许批准范围内的重定时，不原地覆盖 Accepted Snapshot。不能只由 Agent 写一句“源自已接受版本”，而缺乏实际生成关联。

依赖变化的检测针对当前使用内容。无关公共库更新不应使已冻结 Work 无法预览或交付。

### 10.3 规则与自动检查的职责

代码负责确定性检查、产物和路径完整、明确状态及安全边界；Agent 负责设计判断和具体问题修正；用户负责方向性选择与成片接受。

Anti-PPT 按阶段检查：Plan 检查逐 Scene 资产适配和必要缺口样段的信息、文字与阅读；不要求为已适配资产补样段，也不要求静态样段动画 PASS。Draft 检查真实关系、运动和阅读，完整接受不得缺必需效果。“可能更好看”不能使已确认方向自动失效，不增加独立审查与审批。

## 11. 验收场景

自动化测试与人工视觉验收分别报告，不能互相冒充。下列验收是结果要求，不是每条作品都要向用户展示的清单。

| ID | 验收场景 | 通过条件 |
|---|---|---|
| A01 | Plan 尚未批准 | 可登记和接受引用型 Plan 或必要缺口样段，不要求完整工程；不能直接 Finalize |
| A02 | 全部 Scene 已有适配资产 | 无新增样段也能推进；局部样段只覆盖明确范围，不自动变成全片布局 |
| A03 | 高信息密度 | 样段核心信息表达完整、需读文字无无限缩字；Draft 另验实际速度下的阅读 |
| A04 | 文字与动效平衡 | 复杂文字有稳定窗口；运动对象与解释能对应；稳定表格或证据页不因形式被强制返工 |
| A05 | 复用确认部分 | Draft 沿用文字、tokens、样式和有用代码，允许补齐动作和其他 Scene，不要求先补回 Plan |
| A06 | GSAP + Three.js 混合求值 | 同时验证正常播放、直接跳入中间、倒跳、再播放；文字和 WebGL 同帧同步 |
| A07 | 效果时间确定性 | 在相同固定渲染环境按 `[t3, 0, t1, t3]` 等顺序采样，同一时间状态一致；不依赖先播放前面帧 |
| A08 | 分阶段依赖与资源 | layout 占位不加载、probe 或下载，实际使用资源有效；完整 Draft / Final 资源闭合且必需媒体和效果缺失时失败；无抓帧下载或无界资源增长 |
| A09 | 真实或候选复用 | 同一个效果实现用于不同文本或布局绑定，无内部重写；区分技术样例与真实 Work 复用 |
| A10 | 局部改稿 | 修改指定 Scene；未受影响的源码、内容和资产版本不变，受影响交接有检查 |
| A11 | 配音重定时 | 动作、阅读与交接仍合理，无截断音频、删关键信息或整片重新选型 |
| A12 | 旧 Work 与另一工作流 | 旧完整预演与混合渲染继续兼容；旧源码、媒体、接受快照和 Final 不改写，不另起 Work 重做；播客图文无无关变化 |
| A13 | Studio 默认与多实例 | 打开准确工程和实际端口；登记版本使用隔离副本，原快照不变，未静默回退自制 UI |
| A14 | 逐 Scene 语义保真 | 角色、连接、分支条件、去向与反馈按 Plan 可见；未支持类型明确报缺口，不通用卡片兜底 |
| A15 | 独立资产更新 | 新兼容包单独接纳并发现，程序哈希不变；旧 Work 的固定副本、Lock 和输出关联不漂移 |

轻量路径还应验证：锁定 Studio 只承载当前缺口，无新 Three / GSAP 制作、真实音频同步、转码或 MP4；后台不初始化全片。Plan 关联精确资产和必要样段，未验证动作不冒充通过；缺官方入口明确报告而非静默降级。Windows 可先完成缺口方案，WSL 效果未交付不无条件阻塞 Plan。技术验证不能替代真实生产成效。

A07 不要求跨 GPU、驱动、系统或不同渲染器版本的逐字节像素相同。固定环境比较时间状态与关键帧；涉及抗锯齿等差异时采用明确容差，并用真实成片复核。

Three.js 的测试不能只有函数 mock；至少在实际目标环境中做一次混合场景的真实抓帧或短片渲染。若当前执行环境不能完成，报告具体未验证项，不把静态检查写成渲染通过。

## 12. 实施顺序与完成定义

### M1：Studio 默认与逐 Scene 实现

同步替换默认一个样段和样段全片传播规则，先逐 Scene 匹配，仅对真实缺口做实际文字 HTML/CSS 样段，可为零。接通锁定官方 Studio 的准确工程入口与冻结版本隔离；修正实际生成器的关系压平和静默 fallback，不等待资产整库迁移。

完成定义：必要缺口足以判断即提交；其余 Scene 有明确适配方案，局部批准范围不扩散。Draft 按真实关系实现且先验证最不确定镜头；不能用“打开 Studio”替代场景修复证据，没有新必经审批。

### M2：独立资产最短链路

选择一个确有需求的兼容组件或效果，验证 WSL 封装、Windows 隔离导入与精确接纳、自动发现及固定绑定。旧内置库只读兼容，新资产不依赖全库白名单或 42 项整组门；普通新增包不重发 Harness。

完成定义：同身份同版本冲突或所需环境不兼容时拒绝；AssetStore 更新或不可访问不改变旧 Work 的闭合副本。后续高价值 GSAP / Three 效果和完整 Theme / Background 解耦仍按真实需求开发，不作为本轮整库扩建。

### M3：用真实交付检验返工是否下降

完成局部差异查看、配音重定时检查和少量结果记录。基于真实案例调整组件检索信息、可配置边界和需要沉淀的效果；用户继续主导完整组件库的内容选择。

完成定义：可以解释至少一个重复返工问题是否减少，或者为什么没有减少。没有证据时如实保留假设，不继续增加规则掩盖问题。

这三个阶段是实施拆分，不是新增三轮产品审批。Codex 按现有开发授权推进可完成部分；真正需要用户判断的是具体作品的设计选择与已定义的权限边界。

## 13. 成效度量与风险控制

### 13.1 最小观察指标

从少量可比的真实 Work 开始，建议覆盖不同文字密度及至少一种正式音频路径。先记录基线，不立即承诺固定改善百分比。

| 指标 | 定义与用途 |
|---|---|
| 总交付投入 | Windows 与 WSL 从开始制作到最终接受的总投入，Plan 单列；不能仅比较渲染次数或 Plan 耗时 |
| 实际 token | 从新增样段记录可取得的输入/输出及缓存原口径；缺失明确标明，不用代码行数冒充 |
| 布局与动效返工 | 分别记录，避免把后移的风险当作已经消除 |
| Plan 后方向性返工 | 获批后因“实际效果与预期不同”而重做的 Scene / 已确认 Scene；新增需求单独记 |
| 修改影响范围 | 每条反馈实际改了哪些 Scene；观察是否仍因局部问题重做整片 |
| 有效复用 | 沿用公共组件或效果实现，通过绑定完成不同用途；完整组件与效果模块分开统计 |
| 信息保留与阅读问题 | 必须呈现的信息是否遗漏、过度简化或看不清；不能以高复用率掩盖退化 |

记录放入现有 Work 备注或运行元数据即可。不新增必填日报、复杂看板或 Agent 自评分体系。暂停等待用户录制/批准的时间与制作、返工时间区分。

### 13.2 主要风险

- **预演变成另一轮完整制作**：先匹配已有资产，仅验证真实缺口；不设至少一个样段，也不因打开 Studio 补齐全片；够判断即停。
- **追求视觉奇观导致解释变少**：用核心结论是否被正确表达和实际阅读检查约束结果，而不是靠“看起来不像 PPT”。
- **技术复用变成模板同质化**：复用动作机制，内容结构和叙事编排仍按具体主题设计。
- **Three.js 兼容或性能不稳定**：默认在 Draft 最早检查真实镜头，固定依赖并验证就绪与释放；改变核心视觉的替代须局部确认。
- **本地代码已超前于公开基线**：先识别已存在能力，仅补缺口，不重复造轮子或覆盖本地开发。
- **未来设计被误删**：未纳入本轮不等于取消；在原 PRD/索引中保留并标明后续范围。

## 14. 给 Codex 的执行提示词

以下内容可连同本 PRD 一起交给本地 Codex：

```text
请在当前 hyperframes-AI 仓库中，依据本 v2.2 修订迭代现有 Harness，不另起项目、不叠加第三份日常规则。

目标是减少真实返工，提高组件/效果的有效复用，并让 AI 科普、Skill 介绍在文字信息丰富的同时具有解释性动效。不要把目标替换成增加规则、组件数量、动画数量或审查步骤。

先核对本地当前代码、既有 PRD、Work 结构、已安装 HyperFrames/GSAP/Three.js 版本和可用预览/渲染能力。公开审查基线是 fc65f01dbd74b25b6b19ae1019efe1f04a7bb526，但不得回退本地更新。复用已有实现；仅解决本轮直接相关的合同或接线缺口。

先完成 M1：逐 Scene 筛选信息并匹配可用资产，只有真实缺口才做实际文字 HTML/CSS 样段，可为零。用锁定官方 Studio 薄承载样段或打开准确工程，不为时间线补齐全片、不新做 GSAP/Three、不同步正式音频或导出 MP4。沿用 reference/layout/executable 类型与生命周期，正式 Draft/Final 检查不放松。

Plan 区分全片颜色字体、仅对明确 Scene 生效的局部布局、未验证动作和媒体；接受关联准确引用及必要快照，不写 accepted_preview 或放行 Final。Draft 按各 Scene 的关系和 Binding 复用，不把 participants/branch/feedback 压成统一文字行；不支持就报具体缺口。Windows 负责需求和必要样段，新组件/效果主体按需交给 WSL。

文字、素材、操作与动效按信息性质分工；GSAP 组织阅读、焦点和关系，Three.js 用于确有价值的空间机制。不能靠删掉核心信息、无限缩小字体或堆装饰粒子反 PPT。现有 Anti-PPT Skill 改成信息型动效指导，不要求逐字逐卡重复或每场套用同一公式。

M2 复用当前 Component 合同完成外部来源、隔离导入、精确接纳、自动发现和固定绑定，不让兼容新资产依赖 Harness 重发或历史整库门。旧包、旧 Work 与 hash 不变；Profile/Background 完整解耦和后续效果设计保留，不冒充已全部完成。保留混合渲染、GSAP/Three 与依赖闭包，不重建这些能力，静态检查与实际渲染分开报告。

反馈按 Scene/时间范围处理，只修改受影响内容与必要交接。保持旧 Work、冻结资产、原 Accepted Draft 和 Final 不变；作品数据不进 Git，播客图文工作流不做无关改造。

能够合理判断的实现细节自行决定；只有缺失信息会实质改变设计或触及既有授权时才提出聚焦问题。用户素材缺失时完成可以验证的基础能力，明确剩余验证，不把技术样例冒充真实生产成功。

结束时报告：完成的能力、对应文件、实际运行过的验证、仍未验证的部分，以及一条真实 Work 如何从新入口使用。提交、推送、发布和外部动作遵循当前仓库与用户已有授权，不由本 PRD 自动扩大权限。
```

## 15. 依据与技术参考

仓库事实按下列固定提交记录；技术文档于 2026-09-05 查阅。上游文档或代码可能领先于本机安装版本，实施时应再次核对。本 PRD 的设计规则、候选效果和实施阶段是提案，不是上游已经保证的能力。

- [R1] 当前 Animation Plan 模板：`https://github.com/alcor-ming/hyperframes-AI/blob/fc65f01dbd74b25b6b19ae1019efe1f04a7bb526/.studio/templates/ANIMATION_PLAN.template.md`
- [R2] 当前创作工作流：`https://github.com/alcor-ming/hyperframes-AI/blob/fc65f01dbd74b25b6b19ae1019efe1f04a7bb526/.studio/workflow.md`
- [R3] 当前 Anti-PPT Skill：`https://github.com/alcor-ming/hyperframes-AI/blob/fc65f01dbd74b25b6b19ae1019efe1f04a7bb526/.agents/skills/hyperframes-anti-ppt/SKILL.md`
- [R4] 当前主 PRD：`https://github.com/alcor-ming/hyperframes-AI/blob/fc65f01dbd74b25b6b19ae1019efe1f04a7bb526/docs/PRD/PRD_MASTER.md`
- [R5] 当前 Work CLI：`https://github.com/alcor-ming/hyperframes-AI/blob/fc65f01dbd74b25b6b19ae1019efe1f04a7bb526/.studio/work.py`
- [T1] GSAP 可复用效果：`https://gsap.com/docs/v3/GSAP/gsap.registerEffect()/`
- [T2] GSAP Flip：`https://gsap.com/docs/v3/Plugins/Flip/`
- [T3] GSAP seek 与回调：`https://gsap.com/docs/v3/GSAP/Timeline/seek()/`
- [T4] HyperFrames 上游 Three.js 接入说明；本次读取 blob 为 `e6c3b1c5533b074712144a8a945869e14b42728b`：`https://github.com/heygen-com/hyperframes/blob/main/skills/hyperframes-animation/adapters/three.md`
- [T5] Three.js WebGLRenderer：`https://threejs.org/docs/pages/WebGLRenderer.html`
